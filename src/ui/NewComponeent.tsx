import React from "react";
import clsx from "clsx";

type Props = {
  props?: string;
};

const NewComponeent: React.FC<Props> = ({ props }) => {
  return (
    <div>
      <div>1</div>
    </div>
  );
};

export default NewComponeent;
